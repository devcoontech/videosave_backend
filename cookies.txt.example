# cookies.txt — optional for VPS YouTube (often causes IP-mismatch blocks!)
#
# On Contabo/VPS: YouTube works via bgutil PO tokens + android_vr WITHOUT cookies.
# If you see "YouTube blocked this request", REMOVE cookies.txt from the server.
#
# cookies.txt IS useful for:
#   - Local development on your PC
#   - Age-restricted videos (set YOUTUBE_COOKIES_FIRST=true AND cookies from same network)
#   - Facebook / Instagram private content
#
# Coolify env (recommended for VPS):
#   YOUTUBE_COOKIES_FIRST=false
#   BGUTIL_POT_BASE_URL=http://127.0.0.1:4416
#
# Verify: GET /api/health → bgutil_reachable: true, youtube_ready: true
